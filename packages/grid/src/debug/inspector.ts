/**
 * Install a dev-tools inspector that shows cell info on hover.
 * Only active in development mode.
 */
export function installInspector(_canvas: HTMLCanvasElement): () => void {
  throw new Error("TODO: installInspector");
}
