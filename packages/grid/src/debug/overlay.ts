import type { CellLayout } from "../types";

/** Render a debug overlay showing cell layout boundaries on the canvas. */
export function renderDebugOverlay(_ctx: CanvasRenderingContext2D, _layouts: CellLayout[]): void {
  throw new Error("TODO: renderDebugOverlay");
}
